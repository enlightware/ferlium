version: 2
updates:
  # Workflow actions are pinned to commit SHAs; this keeps those pins and their version comments current.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      github-actions:
        patterns:
          - "*"

  # No lockfiles are tracked, so these only raise requirements that exclude new releases.
  # Grouping keeps crates that must move together (e.g. wasm-bindgen, js-sys, web-sys) in one PR.
  # Major versions are adopted deliberately rather than proposed; security alerts are unaffected.
  - package-ecosystem: cargo
    directory: /
    schedule:
      interval: monthly
    ignore:
      - dependency-name: "*"
        update-types: ["version-update:semver-major"]
    groups:
      cargo:
        patterns:
          - "*"

  - package-ecosystem: npm
    directory: /playground
    schedule:
      interval: monthly
    ignore:
      - dependency-name: "*"
        update-types: ["version-update:semver-major"]
    groups:
      npm:
        patterns:
          - "*"
